name: Qt C++

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v1
      
    - name: Install Catch2
      uses: Legion2/download-release-action@v2.1.0
      with:
        repository: catchorg/Catch2
        tag: 'v2.13.10'
        path: /releases/download/v2.13.10/
        file: catch.hpp

    - name: set environment variable
      run: echo "CATCH_INCLUDE_DIR=$(Get-Location)" >> $env:GITHUB_ENV
      
    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        aqtversion: '==3.1.*'
        version: '6.5.0'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_msvc2019_64'
        dir: '${{ github.workspace }}\Remini\' 
        cache: 'true'
        cache-key-prefix: 'install-qt-action'
        setup-python: 'false'
        tools: 'tools_cmake'
        
    - name: Configure CMake
      run: cmake -DCMAKE_BUILD_TYPE=Release -B="Build" -DQt6_DIR="D:\a\Remini\Remini\Remini\Qt\6.5.0\msvc2019_64"

    - name: Build the Qt project with CMake
      run: cmake --build build --config Release

    - name: Run test
      run: |
        cd Build/CatchTests
        ${{ github.workspace }}\Remini\Qt\6.5.0\msvc2019_64\bin\windeployqt.exe ReminiTests.exe
        ReminiTests.exe

        
